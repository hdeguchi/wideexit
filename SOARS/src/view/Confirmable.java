package view;

/**
 * The Confirmable interface represents window with confirmation when closing.
 * @author H. Tanuma / SOARS project
 */
public interface Confirmable {

	public int confirm(Object message);
}
